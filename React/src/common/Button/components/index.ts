import Intent from "common/Button/components/Intent";
import Outline from "common/Button/components/Outline.ts";

const variants = {
    intent: Intent,
    outline: Outline
}

export default variants