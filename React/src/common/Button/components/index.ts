import Intent from "shared/common/Button/components/Intent";
import Outline from "shared/common/Button/components/Outline.ts";

const variants = {
    intent: Intent,
    outline: Outline
}

export default variants